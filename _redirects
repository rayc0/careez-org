# Cloudflare Pages _redirects
# NOTE: Static files always take priority over these rules in Cloudflare Pages.
# The SPA fallback only applies when no matching file exists.

# SPA fallback for client-side routing (404 → index.html)
/404            /index.html     200
